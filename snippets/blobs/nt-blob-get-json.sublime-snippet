<snippet>
    <content><![CDATA[
import { getStore } from '@netlify/blobs';

const store = getStore('${1:my-store}');
const data = await store.get('${2:key}', { type: 'json' });
${0}
]]></content>
    <tabTrigger>nt:blob:get:json</tabTrigger>
    <scope>source.js, source.ts, source.tsx</scope>
    <description>Get blob as JSON</description>
</snippet>
