<snippet>
    <content><![CDATA[
import { getStore } from '@netlify/blobs';

const store = getStore('${1:my-store}');
const { blobs } = await store.list();

for (const blob of blobs) {
  console.log(blob.key);
}
${0}
]]></content>
    <tabTrigger>nt:blob:list</tabTrigger>
    <scope>source.js, source.ts, source.tsx</scope>
    <description>List blobs in store</description>
</snippet>
