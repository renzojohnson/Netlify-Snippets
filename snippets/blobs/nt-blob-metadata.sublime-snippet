<snippet>
    <content><![CDATA[
import { getStore } from '@netlify/blobs';

const store = getStore('${1:my-store}');
await store.set('${2:key}', ${3:value}, {
  metadata: { ${4:contentType}: '${5:application/json}' },
});

const { metadata } = await store.getMetadata('${2:key}');
${0}
]]></content>
    <tabTrigger>nt:blob:metadata</tabTrigger>
    <scope>source.js, source.ts, source.tsx</scope>
    <description>Blob with metadata</description>
</snippet>
