<snippet>
    <content><![CDATA[
import { getStore } from '@netlify/blobs';

const store = getStore('${1:my-store}');
await store.setJSON('${2:key}', ${3:{ data: 'value' }});
${0}
]]></content>
    <tabTrigger>nt:blob:set:json</tabTrigger>
    <scope>source.js, source.ts, source.tsx</scope>
    <description>Set blob as JSON</description>
</snippet>
