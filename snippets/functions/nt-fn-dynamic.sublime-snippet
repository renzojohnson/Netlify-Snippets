<snippet>
    <content><![CDATA[
import type { Context } from '@netlify/functions';

export default async (request: Request, context: Context) => {
  const { ${1:id} } = context.params;
  ${2:// Dynamic route logic}
  return Response.json({ ${1:id} });
};

export const config = {
  path: '/api/${3:users}/:${1:id}',
};
${0}
]]></content>
    <tabTrigger>nt:fn:dynamic</tabTrigger>
    <scope>source.js, source.ts, source.tsx</scope>
    <description>Dynamic route params</description>
</snippet>
