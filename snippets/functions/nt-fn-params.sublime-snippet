<snippet>
    <content><![CDATA[
export default async (request: Request) => {
  const url = new URL(request.url);
  const ${1:id} = url.searchParams.get('${1:id}');
  ${2:// Param logic}
  return Response.json({ ${1:id} });
};

export const config = {
  path: '/${3:api/search}',
};
${0}
]]></content>
    <tabTrigger>nt:fn:params</tabTrigger>
    <scope>source.js, source.ts, source.tsx</scope>
    <description>URL search params extraction</description>
</snippet>
