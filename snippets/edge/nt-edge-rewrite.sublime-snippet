<snippet>
    <content><![CDATA[
export default async (request: Request, context: any) => {
  const url = new URL(request.url);
  url.pathname = '${1:/rewritten-path}';
  return url;
};

export const config = {
  path: '/${2:original-path}',
};
${0}
]]></content>
    <tabTrigger>nt:edge:rewrite</tabTrigger>
    <scope>source.js, source.ts, source.tsx</scope>
    <description>Rewrite request URL (return URL object)</description>
</snippet>
