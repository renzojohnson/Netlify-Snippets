<snippet>
    <content><![CDATA[
import type { Context, Config } from '@netlify/edge-functions';

export default async (request: Request, context: Context) => {
  ${1:// Edge handler logic}
  return new Response('${2:Hello from the edge}');
};

export const config: Config = {
  path: '/${3:*}',
};
${0}
]]></content>
    <tabTrigger>nt:edge:handler</tabTrigger>
    <scope>source.js, source.ts, source.tsx</scope>
    <description>Edge Function handler (typed)</description>
</snippet>
