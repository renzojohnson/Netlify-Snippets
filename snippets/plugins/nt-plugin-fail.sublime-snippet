<snippet>
    <content><![CDATA[
export const onPreBuild = ({ utils }) => {
  const isValid = ${1:false};

  if (!isValid) {
    utils.build.failBuild('${2:Validation failed: missing required config}');
  }
  ${3:// Continue if valid}
};
${0}
]]></content>
    <tabTrigger>nt:plugin:fail</tabTrigger>
    <scope>source.js, source.ts, source.tsx</scope>
    <description>Fail build with message</description>
</snippet>
