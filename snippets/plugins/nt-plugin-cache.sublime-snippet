<snippet>
    <content><![CDATA[
export const onPreBuild = async ({ utils }) => {
  await utils.cache.restore('${1:.cache}');
};

export const onPostBuild = async ({ utils }) => {
  await utils.cache.save('${1:.cache}');
};
${0}
]]></content>
    <tabTrigger>nt:plugin:cache</tabTrigger>
    <scope>source.js, source.ts, source.tsx</scope>
    <description>Cache save/restore pattern</description>
</snippet>
